import { Sparkles } from "lucide-react";

export const HighlightNote = () => (
  <div className="mt-10 flex items-center justify-center gap-2 text-center text-sm text-gray-500 dark:text-gray-400">
    <Sparkles className="text-indigo-400" size={18} />
    We care for your clothes like it's our own. 100% satisfaction guarantee.
  </div>
);

